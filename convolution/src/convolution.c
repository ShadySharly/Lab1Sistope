/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////// DIRECTIVES /////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
# include <stdlib.h>
# include <stdio.h>
# include <unistd.h>
# include <string.h>

# include "../../structs.h"
# include "convolution.h"

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////// FUNCTIONS //////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// - INPUTS:
// - OUTPUTS:
// - DESCRIPTION:

void convolution (Image* image, int mask[3][3]) {



    
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// - INPUTS:
// - OUTPUTS:
// - DESCRIPTION:

Image* defaultImage (int height, int width) {

    int n, m;
    Image* new_image = (Image*)malloc(sizeof(Image));


    if (new_image != NULL) {
        new_image -> matrix = (Pixel**)malloc(sizeof(Pixel) * height);

        if ( (new_image -> matrix) != NULL) {

            for (n = 0; n < height; n++) {
                new_image -> matrix[n] = (Pixel*)malloc(sizeof(Pixel) * width);

                if ( (new_image -> matrix[n]) != NULL) {

                    for (m = 0; m < width; m++) {
                        Pixel default_pixel;
                        default_pixel.red = 0;
                        default_pixel.green = 0;
                        default_pixel.blue = 0;

                        new_image -> matrix[n][m] = default_pixel;
                    }
                }
                else {

                }
            }
        }
        else {

        }
    }
    else {

    }
    
    return new_image;
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// - INPUTS:
// - OUTPUTS:
// - DESCRIPTION:



/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////